# SQL Scripts

This directory contains database-related SQL scripts used by the ELT pipeline.

Examples:
- Schema creation scripts
- Index creation scripts
- Database maintenance queries
- Migration scripts

Current database objects and transformations are primarily managed through PostgreSQL and dbt models.
